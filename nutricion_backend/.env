# Copia este archivo a `.env` y ajústalo si hace falta.
# Nos conectamos con el rol 'app' (no superusuario) para que el RLS aplique.
DATABASE_URL=postgresql+psycopg://app:app@localhost:5433/nutricion
